import Link from "next/link";
import { notFound } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { createTeacherQuestion, deleteTeacherQuestion, deleteTeacherQuestionPage, publishTeacherQuestionPage, reorderTeacherQuestion, saveTeacherAnswer, unpublishTeacherQuestionPage } from "@/lib/teacher-actions";
import { saveTeacherDiscussion } from "@/lib/teacher-discussion-actions";

type Question = { id: string; question_type: string; marks: number; order_index: number; question_image_url: string | null; paper_code: string; paper_question_number: string };
type Answer = { question_id: string; answer_text: string | null; answer_image_url: string | null; correct_option: string | null };
type Subject = { name: string; code: string | null; levels: { name: string; curriculums: { name: string } | null } | null };
type Node = { id: string; name: string; parent_id: string | null };

type SearchParams = { paperCode?: string; paperQuestion?: string; type?: string };

export default async function TeacherQuestionPage({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<SearchParams> }) {
  const { id } = await params;
  const filters = await searchParams;
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return <div className="app-page"><div className="app-page-content"><h1>Sign in required</h1></div></div>;
  const { data: profile } = await supabase.from("profiles").select("role, is_active").eq("id", user.id).maybeSingle();
  if (!profile?.is_active || !["teacher", "admin"].includes(profile.role)) return <div className="app-page"><div className="app-page-content"><h1>Teacher access</h1></div></div>;
  const { data: page } = await supabase.from("question_pages").select("id, subject_id, content_node_id, title, description, page_type, is_published").eq("id", id).maybeSingle();
  if (!page) notFound();
  const { data: assignment } = profile.role === "admin" ? { data: { id: "admin" } } : await supabase.from("teacher_subjects").select("id").eq("teacher_id", user.id).eq("subject_id", page.subject_id).eq("is_active", true).maybeSingle();
  if (!assignment) return <div className="app-page"><div className="app-page-content"><h1>Access denied</h1><p>You are not assigned to this subject.</p></div></div>;
  const [{ data: rawSubject }, { data: rawNode }, { data: rawQuestions }, { data: rawDiscussion }] = await Promise.all([
    supabase.from("subjects").select("name, code, levels(name, curriculums(name))").eq("id", page.subject_id).maybeSingle(),
    page.content_node_id ? supabase.from("content_nodes").select("id, name, parent_id").eq("id", page.content_node_id).maybeSingle() : Promise.resolve({ data: null }),
    supabase.from("questions").select("id, question_type, marks, order_index, question_image_url, paper_code, paper_question_number").eq("question_page_id", id).order("order_index", { ascending: true }),
    supabase.from("question_page_discussions").select("youtube_url").eq("question_page_id", id).maybeSingle(),
  ]);
  const subject = rawSubject as unknown as Subject | null;
  const node = rawNode as Node | null;
  let questions = (rawQuestions ?? []) as Question[];
  const discussion = rawDiscussion as { youtube_url: string } | null;
  const questionIds = questions.map((question) => question.id);
  const { data: rawAnswers } = questionIds.length ? await supabase.from("question_answers").select("question_id, answer_text, answer_image_url, correct_option").in("question_id", questionIds) : { data: [] as Answer[] };
  const answerByQuestion = new Map(((rawAnswers ?? []) as Answer[]).map((answer) => [answer.question_id, answer]));
  const paperCode = filters.paperCode?.trim().toLowerCase() ?? "";
  const paperQuestion = filters.paperQuestion?.trim().toLowerCase() ?? "";
  const questionType = filters.type && ["mcq", "structured", "essay"].includes(filters.type) ? filters.type : "";
  if (paperCode || paperQuestion || questionType) {
    questions = questions.filter((question) => (!paperCode || question.paper_code.toLowerCase().includes(paperCode)) && (!paperQuestion || question.paper_question_number.toLowerCase().includes(paperQuestion)) && (!questionType || question.question_type === questionType));
  }
  const hasFilters = Boolean(paperCode || paperQuestion || questionType);

  return <div className="app-page"><div className="app-page-content max-w-6xl">
    <div className="flex flex-wrap items-start justify-between gap-4">
      <div><Link href="/teacher/content" className="text-sm font-semibold text-primary hover:underline">← Back to Content Manager</Link><p className="app-eyebrow mt-5">Question Page · {page.page_type.toUpperCase()}</p><h1>{page.title}</h1><p className="mt-2 text-sm text-muted-foreground">{subject?.name ?? "Subject"}{node ? ` · ${node.name}` : ""}</p></div>
      <div className="flex flex-wrap items-center justify-end gap-2"><span className={`rounded-full px-3 py-1.5 text-xs font-semibold ${page.is_published ? "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400" : "bg-muted text-muted-foreground"}`}>{page.is_published ? "Published" : "Draft"}</span>{page.is_published ? <form action={unpublishTeacherQuestionPage}><input type="hidden" name="pageId" value={page.id}/><button className="rounded-xl border border-border bg-background px-4 py-2.5 text-sm font-semibold">Unpublish</button></form> : <form action={publishTeacherQuestionPage}><input type="hidden" name="pageId" value={page.id}/><button className="rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Publish Page</button></form>}<form action={deleteTeacherQuestionPage}><input type="hidden" name="pageId" value={page.id}/><button className="rounded-xl border border-destructive/30 bg-destructive/10 px-4 py-2.5 text-sm font-semibold text-destructive">Delete Page</button></form></div>
    </div>
    {!page.is_published && <div className="mt-5 rounded-2xl border border-amber-500/20 bg-amber-500/5 p-4 text-sm text-muted-foreground"><span className="font-semibold text-foreground">Draft:</span> Publishing checks that the page has questions, every question has a paper reference and image, and every MCQ has a correct answer.</div>}
    <section className="mt-8 rounded-2xl border border-border bg-card p-6 shadow-sm"><div><p className="app-eyebrow">New Question</p><h2 className="text-xl font-semibold">Add Question</h2><p className="mt-1 text-sm text-muted-foreground">Question numbers are automatic. Enter the original paper reference and choose the answer while creating an MCQ.</p></div><form action={createTeacherQuestion} className="mt-6 space-y-5"><input type="hidden" name="pageId" value={page.id}/><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><label className="grid gap-1.5"><span className="text-sm font-medium">Paper Code</span><input name="paperCode" required placeholder="e.g. 2024 May" className="rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/></label><label className="grid gap-1.5"><span className="text-sm font-medium">Paper Question Number</span><input name="paperQuestionNumber" required placeholder="e.g. 12(a)(ii)" className="rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/></label><label className="grid gap-1.5"><span className="text-sm font-medium">Question Type</span><select name="questionType" defaultValue={page.page_type === "mcq" ? "mcq" : "structured"} className="rounded-xl border border-border bg-background px-3 py-2.5 text-sm"><option value="mcq">MCQ</option><option value="structured">Structured</option><option value="essay">Essay</option></select></label><label className="grid gap-1.5"><span className="text-sm font-medium">Marks</span><input name="marks" type="number" min="0.5" step="0.5" defaultValue="1" className="rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/></label></div><div className="grid gap-4 lg:grid-cols-[1fr_1fr]"><div className="rounded-xl border border-dashed border-border p-4"><p className="text-sm font-medium">Question Image</p><p className="mt-1 text-xs text-muted-foreground">Upload the paper question, or use an existing image URL.</p><input name="questionImageFile" type="file" accept="image/png,image/jpeg,image/webp,image/gif" className="mt-3 block w-full rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/><input name="questionImageUrl" placeholder="Optional image URL" className="mt-3 block w-full rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/></div><div className="rounded-xl border border-border bg-muted/20 p-4"><p className="text-sm font-medium">Correct Answer</p><p className="mt-1 text-xs text-muted-foreground">For MCQ, select the correct option from the paper.</p><select name="correctOption" defaultValue="" className="mt-3 block w-full rounded-xl border border-border bg-background px-3 py-2.5 text-sm"><option value="">Not set</option><option value="A">A</option><option value="B">B</option><option value="C">C</option><option value="D">D</option></select></div></div><div className="flex justify-end"><button className="rounded-xl bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground">Add Question</button></div></form></section>
    <section className="mt-8 rounded-2xl border border-border bg-card p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="app-eyebrow">Discussion</p><h2 className="text-xl font-semibold">Discussion Video</h2><p className="mt-1 text-sm text-muted-foreground">Add the YouTube video that explains this question page.</p></div>{discussion?.youtube_url && <span className="rounded-full bg-emerald-500/10 px-3 py-1.5 text-xs font-semibold text-emerald-600 dark:text-emerald-400">Video linked</span>}</div><form action={saveTeacherDiscussion} className="mt-5 flex flex-col gap-3 sm:flex-row"><input type="hidden" name="pageId" value={page.id}/><input name="youtubeUrl" defaultValue={discussion?.youtube_url ?? ""} placeholder="https://www.youtube.com/watch?v=..." className="min-w-0 flex-1 rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/><button className="rounded-xl bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground">Save Video</button></form>{discussion?.youtube_url && <p className="mt-2 text-xs text-muted-foreground">Clear the URL and save to remove the discussion video.</p>}</section>
    <section className="mt-8 rounded-2xl border border-border bg-card p-6 shadow-sm"><div className="flex items-center justify-between gap-3"><div><p className="app-eyebrow">Filter</p><h2 className="text-xl font-semibold">Find Questions</h2><p className="mt-1 text-sm text-muted-foreground">Filter this Question Page by the original paper reference.</p></div>{hasFilters && <span className="rounded-full bg-primary/10 px-3 py-1.5 text-xs font-semibold text-primary">{questions.length} match{questions.length === 1 ? "" : "es"}</span>}</div><form method="get" className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-[1fr_1fr_180px_auto]"><input name="paperCode" defaultValue={filters.paperCode ?? ""} placeholder="Paper code e.g. 2024 May" className="rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/><input name="paperQuestion" defaultValue={filters.paperQuestion ?? ""} placeholder="Paper question e.g. 12(a)(ii)" className="rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/><select name="type" defaultValue={filters.type ?? ""} className="rounded-xl border border-border bg-background px-3 py-2.5 text-sm"><option value="">All types</option><option value="mcq">MCQ</option><option value="structured">Structured</option><option value="essay">Essay</option></select><div className="flex gap-2"><button className="rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Filter</button>{hasFilters && <Link href={`/teacher/question-pages/${page.id}`} className="rounded-xl border border-border px-4 py-2.5 text-sm font-semibold">Clear</Link>}</div></form></section>
    <section className="mt-8 space-y-5"><div><p className="app-eyebrow">Page Questions</p><h2 className="text-xl font-semibold">{questions.length} {questions.length === 1 ? "Question" : "Questions"}</h2></div>{questions.map((question, index) => { const answer = answerByQuestion.get(question.id); return <article key={question.id} className="overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><header className="flex flex-wrap items-center justify-between gap-4 border-b border-border bg-muted/30 px-5 py-4"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10 font-bold text-primary">{index + 1}</span><div><h3 className="font-semibold">Question {index + 1}</h3><p className="text-xs text-muted-foreground">{question.paper_code} · {question.paper_question_number} · {question.marks} marks</p></div></div><div className="flex items-center gap-2"><form action={reorderTeacherQuestion}><input type="hidden" name="questionId" value={question.id}/><input type="hidden" name="direction" value="up"/><button disabled={index === 0} className="rounded-lg border border-border px-3 py-1.5 text-sm disabled:opacity-40" title="Move up">↑</button></form><form action={reorderTeacherQuestion}><input type="hidden" name="questionId" value={question.id}/><input type="hidden" name="direction" value="down"/><button disabled={index === questions.length - 1} className="rounded-lg border border-border px-3 py-1.5 text-sm disabled:opacity-40" title="Move down">↓</button></form><form action={deleteTeacherQuestion}><input type="hidden" name="questionId" value={question.id}/><button className="rounded-lg border border-destructive/30 bg-destructive/10 px-3 py-1.5 text-sm font-semibold text-destructive">Delete</button></form></div></header><div className="p-5">{question.question_image_url ? <img src={question.question_image_url} alt={`Question ${index + 1}`} className="max-h-[500px] w-full rounded-xl border border-border object-contain"/> : <div className="rounded-xl border border-dashed border-border p-8 text-center text-sm text-muted-foreground">No question image added.</div>}<form action={saveTeacherAnswer} className="mt-5 rounded-xl border border-border bg-background p-4"><input type="hidden" name="questionId" value={question.id}/><div className="flex items-center justify-between"><div><h4 className="font-semibold">{question.question_type === "mcq" ? "Correct Answer" : "Model Answer"}</h4><p className="text-xs text-muted-foreground">{question.question_type === "mcq" ? "Change the correct option if needed." : "Add or update the model answer."}</p></div>{answer && <span className="text-xs font-semibold text-emerald-600">Saved</span>}</div>{question.question_type === "mcq" ? <select name="correctOption" defaultValue={answer?.correct_option ?? ""} className="mt-3 w-full max-w-xs rounded-xl border border-border bg-card px-3 py-2.5 text-sm"><option value="">Not set</option><option value="A">A</option><option value="B">B</option><option value="C">C</option><option value="D">D</option></select> : <div className="mt-3 grid gap-3 md:grid-cols-2"><textarea name="answerText" defaultValue={answer?.answer_text ?? ""} rows={5} placeholder="Write the model answer..." className="rounded-xl border border-border bg-card px-3 py-2.5 text-sm"/><input name="answerImageFile" type="file" accept="image/png,image/jpeg,image/webp,image/gif" className="rounded-xl border border-border bg-card px-3 py-2.5 text-sm"/></div>}<button className="mt-3 rounded-xl border border-border bg-card px-4 py-2.5 text-sm font-semibold">Save Answer</button></form></div></article>; })}{!questions.length && <div className="rounded-2xl border border-dashed border-border p-10 text-center text-sm text-muted-foreground">No questions match the current filters.</div>}</section>
  </div></div>;
}