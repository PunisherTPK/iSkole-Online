"use server";
import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";
type SupabaseServerClient = Awaited<ReturnType<typeof createClient>>;
async function getTeacher(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("You must be signed in.");const {data:profile}=await supabase.from("profiles").select("role, is_active").eq("id",user.id).maybeSingle();if(!profile?.is_active||!["teacher","admin"].includes(profile.role))throw new Error("Teacher access required.");return{supabase,user,isAdmin:profile.role==="admin"};}
export async function createTeacherContent(formData:FormData){const{supabase,user}=await getTeacher();const subjectId=String(formData.get("subjectId")||"");const parentId=String(formData.get("parentId")||"")||null;const name=String(formData.get("name")||"").trim();if(!subjectId||!name)throw new Error("Subject and folder name are required.");if(!(await hasSubjectAccess(supabase,user.id,subjectId)))throw new Error("You are not assigned to this subject.");if(parentId){const{data:parent}=await supabase.from("content_nodes").select("id, subject_id").eq("id",parentId).maybeSingle();if(!parent||parent.subject_id!==subjectId)throw new Error("Invalid parent folder.");}const{error}=await supabase.from("content_nodes").insert({subject_id:subjectId,parent_id:parentId,name,created_by:user.id,is_active:true});if(error)throw new Error(error.message);revalidatePath("/teacher/content");}
export async function deleteTeacherContent(formData:FormData){const{supabase,user}=await getTeacher();const id=String(formData.get("id")||"");if(!id)throw new Error("Content folder is required.");const{data:node}=await supabase.from("content_nodes").select("id, subject_id").eq("id",id).maybeSingle();if(!node||!(await hasSubjectAccess(supabase,user.id,node.subject_id)))throw new Error("You cannot modify this content.");const{error}=await supabase.from("content_nodes").delete().eq("id",id);if(error)throw new Error(error.message);revalidatePath("/teacher/content");}
export async function createTeacherQuestionPage(formData:FormData){const{supabase,user}=await getTeacher();const subjectId=String(formData.get("subjectId")||"");const contentNodeId=String(formData.get("contentNodeId")||"").trim()||null;const title=String(formData.get("title")||"").trim();const description=String(formData.get("description")||"").trim()||null;const pageType=String(formData.get("pageType")||"");if(!subjectId||!title||!["mcq","structured"].includes(pageType))throw new Error("Invalid question page data.");if(!(await hasSubjectAccess(supabase,user.id,subjectId)))throw new Error("You are not assigned to this subject.");if(contentNodeId){const{data:node}=await supabase.from("content_nodes").select("id, subject_id").eq("id",contentNodeId).maybeSingle();if(!node||node.subject_id!==subjectId)throw new Error("Invalid folder.");}const{data,error}=await supabase.from("question_pages").insert({subject_id:subjectId,content_node_id:contentNodeId,title,description,page_type:pageType,is_published:false,created_by:user.id}).select("id").single();if(error)throw new Error(error.message);revalidatePath("/teacher/content");return data.id;}
export async function deleteTeacherQuestionPage(formData:FormData){const{supabase,user}=await getTeacher();const pageId=String(formData.get("pageId")||"");if(!pageId)throw new Error("Question page is required.");const{data:page}=await supabase.from("question_pages").select("id, subject_id").eq("id",pageId).maybeSingle();if(!page||!(await hasSubjectAccess(supabase,user.id,page.subject_id)))throw new Error("You cannot delete this question page.");const{error}=await supabase.from("question_pages").delete().eq("id",pageId);if(error)throw new Error(error.message);revalidatePath("/teacher/content");}
export async function createTeacherQuestion(formData:FormData){const{supabase,user}=await getTeacher();const pageId=String(formData.get("pageId")||"");const questionImageUrl=String(formData.get("questionImageUrl")||"").trim()||null;const questionImageFile=formData.get("questionImageFile");const questionType=String(formData.get("questionType")||"");const paperCode=String(formData.get("paperCode")||"").trim();const paperQuestionNumber=String(formData.get("paperQuestionNumber")||"").trim();const correctOptionRaw=String(formData.get("correctOption")||"").trim().toUpperCase();const correctOption=correctOptionRaw||null;const marks=Number(formData.get("marks")||1);if(!pageId||!["mcq","structured","essay"].includes(questionType))throw new Error("Invalid question data.");if(!paperCode||!paperQuestionNumber)throw new Error("Paper code and paper question number are required.");if(!Number.isFinite(marks)||marks<=0)throw new Error("Marks must be greater than zero.");if(correctOption&&!["A","B","C","D"].includes(correctOption))throw new Error("Correct option must be A, B, C, or D.");if(questionType==="mcq"&&!correctOption)throw new Error("Select the correct answer for this MCQ.");const{data:page}=await supabase.from("question_pages").select("id, subject_id").eq("id",pageId).maybeSingle();if(!page||!(await hasSubjectAccess(supabase,user.id,page.subject_id)))throw new Error("You cannot modify this question page.");let finalImageUrl=questionImageUrl;if(questionImageFile instanceof File&&questionImageFile.size>0){if(!questionImageFile.type.startsWith("image/"))throw new Error("Question image must be an image file.");if(questionImageFile.size>8*1024*1024)throw new Error("Question image must be 8 MB or smaller.");const ext=questionImageFile.name.split(".").pop()?.toLowerCase().replace(/[^a-z0-9]/g,"")||"jpg";const extension=["jpg","jpeg","png","webp","gif"].includes(ext)?ext:"jpg";const path=`${user.id}/${pageId}/${crypto.randomUUID()}.${extension}`;const{error:uploadError}=await supabase.storage.from("question-images").upload(path,questionImageFile,{contentType:questionImageFile.type,cacheControl:"31536000",upsert:false});if(uploadError)throw new Error(uploadError.message);finalImageUrl=supabase.storage.from("question-images").getPublicUrl(path).data.publicUrl;}const{data:lastOrder}=await supabase.from("questions").select("order_index").eq("question_page_id",pageId).order("order_index",{ascending:false}).limit(1).maybeSingle();const nextOrder=(lastOrder?.order_index??0)+1;const{data:inserted,error}=await supabase.from("questions").insert({question_page_id:pageId,question_type:questionType,marks,order_index:nextOrder,question_image_url:finalImageUrl,paper_code:paperCode,paper_question_number:paperQuestionNumber}).select("id").single();if(error)throw new Error(error.message);if(questionType==="mcq"&&correctOption){const{error:answerError}=await supabase.from("question_answers").upsert({question_id:inserted.id,correct_option:correctOption},{onConflict:"question_id"});if(answerError){await supabase.from("questions").delete().eq("id",inserted.id);throw new Error(answerError.message);}}revalidatePath(`/teacher/question-pages/${pageId}`);revalidatePath("/teacher/content");return inserted.id;}
export async function deleteTeacherQuestion(formData:FormData){const{supabase,user}=await getTeacher();const questionId=String(formData.get("questionId")||"");if(!questionId)throw new Error("Question is required.");const{data:question}=await supabase.from("questions").select("id, question_page_id, question_pages(subject_id)").eq("id",questionId).maybeSingle();const relation=question?.question_pages as {subject_id:string}|{subject_id:string}[]|null;const subjectId=Array.isArray(relation)?relation[0]?.subject_id:relation?.subject_id;if(!question||!subjectId||!(await hasSubjectAccess(supabase,user.id,subjectId)))throw new Error("You cannot delete this question.");const{error}=await supabase.from("questions").delete().eq("id",questionId);if(error)throw new Error(error.message);await normalizeQuestionOrder(supabase,question.question_page_id);revalidatePath(`/teacher/question-pages/${question.question_page_id}`);}
export async function reorderTeacherQuestion(formData:FormData){const{supabase,user}=await getTeacher();const questionId=String(formData.get("questionId")||"");const direction=String(formData.get("direction")||"");if(!questionId||!["up","down"].includes(direction))throw new Error("Invalid reorder request.");const{data:current}=await supabase.from("questions").select("id, question_page_id, order_index, question_pages(subject_id)").eq("id",questionId).maybeSingle();const relation=current?.question_pages as {subject_id:string}|{subject_id:string}[]|null;const subjectId=Array.isArray(relation)?relation[0]?.subject_id:relation?.subject_id;if(!current||!subjectId||!(await hasSubjectAccess(supabase,user.id,subjectId)))throw new Error("You cannot reorder this question.");const{data:questions}=await supabase.from("questions").select("id, order_index").eq("question_page_id",current.question_page_id).order("order_index",{ascending:true});const ordered=(questions??[]) as {id:string;order_index:number}[];const index=ordered.findIndex(item=>item.id===current.id);const neighborIndex=direction==="up"?index-1:index+1;if(index<0||neighborIndex<0||neighborIndex>=ordered.length)return;const neighbor=ordered[neighborIndex];const temporaryOrder=-1000000-Math.floor(Math.random()*100000);let result=await supabase.from("questions").update({order_index:temporaryOrder}).eq("id",current.id);if(result.error)throw new Error(result.error.message);result=await supabase.from("questions").update({order_index:current.order_index}).eq("id",neighbor.id);if(result.error)throw new Error(result.error.message);result=await supabase.from("questions").update({order_index:neighbor.order_index}).eq("id",current.id);if(result.error)throw new Error(result.error.message);revalidatePath(`/teacher/question-pages/${current.question_page_id}`);}
export async function saveTeacherAnswer(formData:FormData){const{supabase,user}=await getTeacher();const questionId=String(formData.get("questionId")||"");const answerText=String(formData.get("answerText")||"").trim()||null;const correctOptionRaw=String(formData.get("correctOption")||"").trim().toUpperCase();const correctOption=correctOptionRaw||null;const answerImageFile=formData.get("answerImageFile");if(!questionId)throw new Error("Question is required.");if(correctOption&&!["A","B","C","D"].includes(correctOption))throw new Error("Correct option must be A, B, C, or D.");const{data:question}=await supabase.from("questions").select("id, question_page_id, question_pages(subject_id)").eq("id",questionId).maybeSingle();const pageRelation=question?.question_pages as {subject_id:string}|{subject_id:string}[]|null;const subjectId=Array.isArray(pageRelation)?pageRelation[0]?.subject_id:pageRelation?.subject_id;if(!question||!subjectId||!(await hasSubjectAccess(supabase,user.id,subjectId)))throw new Error("You cannot modify this answer.");let answerImageUrl:null|string=null;if(answerImageFile instanceof File&&answerImageFile.size>0){if(!answerImageFile.type.startsWith("image/"))throw new Error("Answer image must be an image file.");if(answerImageFile.size>8*1024*1024)throw new Error("Answer image must be 8 MB or smaller.");const ext=answerImageFile.name.split(".").pop()?.toLowerCase().replace(/[^a-z0-9]/g,"")||"jpg";const extension=["jpg","jpeg","png","webp","gif"].includes(ext)?ext:"jpg";const path=`${user.id}/${question.question_page_id}/answers/${questionId}-${crypto.randomUUID()}.${extension}`;const{error:uploadError}=await supabase.storage.from("question-images").upload(path,answerImageFile,{contentType:answerImageFile.type,cacheControl:"31536000",upsert:false});if(uploadError)throw new Error(uploadError.message);answerImageUrl=supabase.storage.from("question-images").getPublicUrl(path).data.publicUrl;}const{error}=await supabase.from("question_answers").upsert({question_id:questionId,answer_text:answerText,answer_image_url:answerImageUrl,correct_option:correctOption},{onConflict:"question_id"});if(error)throw new Error(error.message);revalidatePath(`/teacher/question-pages/${question.question_page_id}`);}
async function normalizeQuestionOrder(supabase:SupabaseServerClient,pageId:string){const{data:questions}=await supabase.from("questions").select("id").eq("question_page_id",pageId).order("order_index",{ascending:true});for(let index=0;index<(questions??[]).length;index+=1){const{error}=await supabase.from("questions").update({order_index:index+1}).eq("id",questions![index].id);if(error)throw new Error(error.message);}}
async function hasSubjectAccess(supabase:SupabaseServerClient,userId:string,subjectId:string){const{data:profile}=await supabase.from("profiles").select("role, is_active").eq("id",userId).maybeSingle();if(profile?.is_active&&profile.role==="admin")return true;const{data}=await supabase.from("teacher_subjects").select("id").eq("teacher_id",userId).eq("subject_id",subjectId).eq("is_active",true).maybeSingle();return Boolean(data);}
